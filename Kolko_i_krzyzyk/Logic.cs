using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;

namespace Kolko_i_krzyzyk
{
    public static class Logic
    {
        
       
/*
        public static void klikniecia (string nrPola)
        {
            if (nrPola == "button00")
            {
                plansza [0, 0] = "X";
            }

            if (nrPola == "button01")
            {
                plansza[0, 1] = "X";
            }

            if (nrPola == "button02")
            {
                plansza[0, 2] = "X";
            }

            if (nrPola == "button10")
            {
                plansza[1, 0] = "X";
            }

            if (nrPola == "button11")
            {
                plansza[1, 1] = "X";
            }

            if (nrPola == "button12")
            {
                plansza[1, 2] = "X";
            }

            if (nrPola == "button20")
            {
                plansza[2, 0] = "X";
            }

            if (nrPola == "button21")
            {
                plansza[2, 1] = "X";
            }

            if (nrPola == "button22")
            {
                plansza[2, 2] = "X";
            }

        }

        public static void rysowaniePlanszy()
        {
            
            for (int wiersz = 0; wiersz < 3; wiersz++) // w tym miejscu będziemy przeglądać pierwszy wymiar tablicy (wiersze)
            {
                for (int kolumna = 0; kolumna < 3; kolumna++) // ta pętla przegląda elementy w danym wierszu
                {
                    ///plansza[wiersz, kolumna].text = i * j;
                    //Button.
                }
            }

        }

*/
    }
}
